# 线上配置文件
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/team_platform
    username: root
    password: 123456
  # session 失效时间
  session:
    timeout: 86400
server:
  address: 0.0.0.0

